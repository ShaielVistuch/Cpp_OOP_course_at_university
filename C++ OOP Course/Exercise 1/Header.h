#pragma once

#include <stdio.h>

int* createAndInput(int& size);
int pow(int a=1, int b=1);
float pow(float a, int b);
double pow(double a, int b);
int** createAndInput2(int& rows, int& cols);
